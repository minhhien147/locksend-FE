# Sao chép thành .env.local
# Local backend (uvicorn --port 8000):
VITE_API_URL=/api

# Hoặc gọi thẳng (cần ALLOWED_ORIGINS có http://localhost:5173 trong backend/.env):
# VITE_API_URL=http://localhost:8000
