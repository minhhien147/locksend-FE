# Sao chép thành .env.local (dev) — trên Vercel đặt trong Project → Environment Variables
# URL backend public (không có /api suffix — axios gọi thẳng /auth, /upload, …)
VITE_API_URL=http://localhost:8000
# Vercel (Production): VITE_API_URL=https://locksend-be-production-xxxx.up.railway.app
