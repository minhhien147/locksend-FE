# Sao chép thành .env.local (dev)
# Dev qua Vite proxy: VITE_API_URL=/api  (xem vite.config.ts)
# Hoặc gọi thẳng backend: VITE_API_URL=http://localhost:8000

# Railway (frontend service → Variables, build-time):
# VITE_API_URL=https://locksend-be-production-xxxx.up.railway.app
